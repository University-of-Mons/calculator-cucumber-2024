package back.calculator;

public enum ComplexForm {

    /**
     * sqrt(2) + sqrt(2)i
     */
    CARTESIAN,

    /**
     * 2 (cosine(pi/4) + i sine (pi/4))
     */
    POLAR,

    /**
     * 2 e^(i*pi/4)
     */
    EXPONENTIAL

}
