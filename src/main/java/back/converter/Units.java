package back.converter;

public class Units {
    public enum Speed {
        METERS_PER_SECOND,
        METERS_PER_HOUR,
        KILOMETER_PER_SECOND,
        KILOMETER_PER_HOUR,
    }

    public enum Weight {
        GRAM,
        KILOGRAM
    }

    public enum Distance {
        METER,
        KILOMETER
    }

    public enum Time {
        SECOND,
        HOUR
    }
}
